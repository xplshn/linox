We track the Linux kernel's stable releases. We apply to them the latest Oasis Linux patches. We tag a release everytime that the patches apply succesfully to the new kernel release and we confirm the kernel builds.
The branch you're interested in is `linox`.
